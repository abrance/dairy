> shell set 用法
	https://www.ruanyifeng.com/blog/2017/11/bash-set.html
	set -euxo pipefail

> debian10 设置允许 root 用户终端密码登录
	>> 默认情况不允许 root 用户终端密码登录，不过可以设置
------------------------------------------------------------
cat /etc/ssh/sshd_config  | grep PermitRootLogin

sudo systemctl restart sshd
------------------------------------------------------------
