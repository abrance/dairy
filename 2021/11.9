TODO
> 面试python
> 面试 前端
> 了解 go 对docker k8s devops的支持

> 进程、线程、协程
> python协程的使用
	>> 是一种用户态的轻量级线程，协程的调度完全由用户控制。协程拥有自己的寄存器上下文和栈。 协程调度切换时，将寄存器上下文和栈保存到其他地方，在切回来的时候，恢复先前保存的寄存器上下文和栈，直接操作栈则基本没有内核切换的开销，可以不加锁的访问全局变量，所以上下文的切换非常快
> 可变不可变对象
	>> 数值 字符串 元组 不可变 列表 字典 集合 可变
> gc机制
> 闭包 装饰器 如何实现
> 迭代器和生成器

